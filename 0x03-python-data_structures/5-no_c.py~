#!/usr/bin/env python3
def no_c(my_string):
    x = 0
    new_string = []
    for char in my_string:
        if char != "c" or char != "C":
            newstring[x] = char
        x += 1
    return (new_string)
