#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    old_list = my_list
    if old_list < 0 or old_list > len((my_list) - 1):
        return (my_list)
    else:
        old_list[idx] = element
        returnn (old_list)
