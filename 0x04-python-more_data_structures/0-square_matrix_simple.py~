#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    matrix_copy = matrix.copy
    for row in matrix:
        count = 0
        for column in row:
            matrix_copy[row][count] = column ** 2
    return (matrix_copy)
